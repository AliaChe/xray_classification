data:
  path: data/raw/chest_xray/train
  image_size: 224
  validation_split: 0.2

training:
  batch_size: 32
  epochs: 20
  learning_rate: 0.001
  seed: 42
  patience: 3
  dropout: 0.2

paths:
  artifacts: artifacts

model:
  architecture: MobileNetV2